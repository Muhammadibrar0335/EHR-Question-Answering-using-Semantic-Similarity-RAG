# Intelligent EHR Question Answering using Semantic Similarity & RAG

This project implements a medical question-answering system using Semantic Similarity and Retrieval-Augmented Generation (RAG) on Electronic Health Records (EHR) data.

## Folder Structure
- data/: contains raw and cleaned datasets
- notebooks/: contains analysis notebooks
- src/: source code files for preprocessing, embedding, retrieval, and generation
- models/: trained models, embeddings, and indexes
- app/: simple demo interface

## Setup
1. Create virtual environment:
   python -m venv venv
   .\venv\Scripts\Activate.ps1
2. Install dependencies:
   pip install -r requirements.txt
3. Run preprocessing and model scripts.

# Intelligent EHR QA using Semantic Similarity & RAG

## Project Description
An AI-powered question answering system for Electronic Health Records (EHRs) using Semantic Similarity (BERT/SBERT) and Retrieval-Augmented Generation (RAG).

## How to Run
1. Activate virtual environment:
   ```powershell
   .\venv\Scripts\Activate.ps1


# 🧠 Intelligent EHR Question Answering using Semantic Similarity & RAG

## 📘 Overview
This project builds a **Retrieval-Augmented Generation (RAG)** pipeline for answering medical questions from **Electronic Health Records (EHRs)**.  
It combines **semantic embeddings**, **FAISS-based retrieval**, and **Flan-T5** generation to provide factual, explainable answers.

---

## 🗂️ Folder Structure